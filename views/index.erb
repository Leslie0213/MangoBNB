<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mangobnb</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>

  <div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
        <h1>mangobnb</h1>
      </a>

      <div class="col-md-3 text-end">
        <button type="button" class="btn btn-outline-primary me-2">About</button>
        <a href="/login">
        <button type="button" class="btn btn-primary">Login</button>
        </a>
      </div>
    </header>
  </div>
  
  <div class="container text-center">
    <h1>Feel at home, anywhere</h1>
  </div>
  

  <div class="container justify-content-center">
    <form id="form" method="POST" action="/users">
      <div class="mb-3">
        <label for="exampleInputEmail1" class="form-label">Email address</label>
        <input type="email" name="email" class="form-control" id="email" aria-describedby="emailHelp" minlength="5" maxlength="30" required>
        <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
      </div>
      <div class="mb-3">
        <label for="pass_word" class="form-label">Password</label>
        <input type="password" name="pass_word" class="form-control" id="pass_word" minlength="4" maxlength="12" required>
      </div>
      <div class="mb-3">
        <label for="pass_word_confirmation" class="form-label">Confirm Password</label>
        <input type="password" class="form-control" id="pass_word_confirmation" minlength="4" maxlength="12" required>
      </div>
      <button type="submit" class="btn btn-primary">Sign up</button>
<%# 
      <script>
          function BothFieldsIdentical() {
            const form = document.getElementById('form');
            const one = document.forms[0].children[1].children['pass_word'].value;
            const another = document.forms[0].children[2].children['pass_word_confirmation'].value;
            if (one == another) {
                return true;
              } else {
                form.addEventListener('submit', event => {
                  event.preventDefault();
                   alert("Oops, both fields must be identical.");
                   return false;
                });
              }
            }
      </script> %>
    </form> 
  </div>
  
</body>
</html>