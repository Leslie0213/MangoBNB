<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>List all Spaces</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  </head>
  <body>

  <div class="container">
  <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
    <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
      <h1 style="color:orange;">MangoBnB</h1>
    </a>

    <div class="col-md-3 d-flex align-items-baseline justify-content-center">
      <p>Welcome, <a style="color:green;"><%= @user_name %></a></p>
    </div>

  </header>
</div>
<div class="container text-center">
<h1 style="color:indigo;">Spaces available for booking</h1>
</div>
<div class="container">
<!-- adds a link to list new space on the space listing page -->
<h2 style="text-align: right; display: block;"><a href="/spaces/new">List New Space</a></h2>
 
<% @spaces.each do |space| %> 
  <!--<form method="POST" action="/date?name=<%= space.name %>&description=<%= space.description %>&price=<%= space.price %>&from=<%= space.available_from %>&to=<%= space.available_to %>&id=<%= space.user_id %>"> -->
      <div class="card" style="width: 58rem;">
      <img class="card-img-top" src="..." alt="Space image">
      <div class="card-body">
        <h5 class="card-title" name="name"><%= space.name %></h5>
        <p class="card-text"><%= space.description %></p>
        <p class="card-text">Price : £<%= space.price %></p>
        <!--<p class="card-text">Available From : <%= space.available_from %></p>
        <p class="card-text">Available To : <%= space.available_to %></p>
        <p class="card-text">User ID<%= space.user_id %></p>-->
        <a href="/date?name=<%= space.name %>&description=<%= space.description %>&price=<%= space.price %>&from=<%= space.available_from %>&to=<%= space.available_to %>&id=<%= space.user_id %>" class="btn btn-primary">Book Now</a> 
       <!-- <button type="submit" class="btn btn-primary">Book Now</button> -->
      </div>
    </div>
    <!-- </form> -->
<% end %>

</div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
  </body>
</html>